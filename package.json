{
  "name": "js-cypress",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "cypress run",
    "test:rec": "cypress run --record --key $(grep CYPRESS_KEY .env | cut -d '=' -f2)",
    "test:dev": "cypress open"
  },
  "devDependencies": {
    "cypress": "^14.0.3",
    "dotenv": "^16.4.7"
  }
}
